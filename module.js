//xyz = require('./student');
//console.log(xyz);  print the whole object as an object with its property

const {student, classes} = require('./student');
console.log(student, classes);

const os =require('os');
console.log(os.platform(), os.homedir());

